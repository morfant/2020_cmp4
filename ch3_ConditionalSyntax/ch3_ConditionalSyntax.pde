boolean isRunning = true;

void setup() {
  size(400, 400);
  
  isRunning = false;
  
  if (isRunning == true) {
    //
  } else if (isRunning == false){
    //
  } else if (isRunning != true) {
    //
  } else {
    //
  }
}
